/*
Condominios: Criam caminhos no código que permitem criar seleções de bloco específicos de códigos.
Para esse tipo de bikfurcação utilizamos os comandos:

IF (se) --> Para este comando ser executado, precisamos que a operação relacional e/ou lógica seja TRUE

ELSE (SENÃO) --> Para este comando ser executado, precisamos que a operação do IF seja falsa.

*/

let idade = 18;
if(idade >= 16){
    alert("Pode votar");
}else {
    alert("Não pode votar");
}